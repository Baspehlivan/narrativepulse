# NarrativePulse

NarrativePulse is a Python package for analyzing writing style in text files.

Current status: package scaffold is ready. Core analysis features will be added in the next commits.

## Run

```bash
python -m narrativepulse --help
```
